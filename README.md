# discord-mail-bridge

Bridges a discord channel to a mailing list/mailbox, by sending a digest email whenever called.

This is currently deployed on `carbon.hacklab` via `scripts/deploy.sh` (which uses the service files in `contrib/`).

It's pretty minimal, feel free to send PRs.
